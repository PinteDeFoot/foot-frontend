import * as React from 'react';
import { Box, Text } from '@chakra-ui/react';

export const Logo = () => {
  return (
    <Box>
      <Text fontWeight={'black'} fontSize={['18px', '28px']} letterSpacing={'-1px'}>
        Pinte de Foot
      </Text>
    </Box>
  );
};
